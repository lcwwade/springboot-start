# server
server:
  port: 8080

#
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mmall?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456

# Mybatis配置
mybatis:
  mapper-locations:
    - classpath:mapper/*.xml
    - classpath*:com/**/mapper/*.xml

# swagger配置
# http://localhost:8080/swagger-ui.html
swagger:
  base:
    package: com.zero.gogo.concontroller
  description: ''
  title: spring-boot-zero  接口文档
  version: 1.0